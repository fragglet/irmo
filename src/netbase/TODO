Split out all berkeley sockets code into a distinct module.
net/ API needs to be changed to use module references.
Example modules for other types of communication?
Cut out everything irrelevant from IrmoPacket (sock, src, client, flags)

