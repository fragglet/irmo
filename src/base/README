
This directory contains base code on which other modules are based.  This
includes:

    callback.[ch]         - Common callback function data structure, for
                            handling lists of callback functions to invoke.
                            This is exposed publicly as IrmoCallback.
    error.[ch]            - Error reporting.
    iterator.[ch]         - Common iterator data structure, for iterating
                            over collections of objects.  This is exposed
                            publicly as IrmoIterator.
    util.[ch]             - Miscellaneous common utility functions:
                            allocating memory, assertation checking, etc.

